{
  "name": "daypulse-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "consumer": "npm run start --workspace=daypulse",
    "gym": "npm run start --workspace=daypulse-gym"
  }
}
